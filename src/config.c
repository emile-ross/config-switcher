#include "header.h"

static const config_contents program_configs[] = 
{
	/* { "default", "old-name.conf", "new-name.conf" }, */

	/* example: the following example will make a pointer from 
	 * the hyprland.conf file to the hyprland-riced.conf file
	 * { "default", "hyprland-riced.conf", "hyprland.conf" }, 
	 *
	 * You can add multiple files with the same identifier */

};
