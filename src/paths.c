#include "header.h"

/* this is the path to the configuration file from the home path 
 * default: .config 
 * do NOT include the slashes at both ends of the string */
const char *path_to_config = ".config";
